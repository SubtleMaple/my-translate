# 项目进度

> 本文件用于跨阶段交接。每完成一个 Phase 必须更新：状态表、完成记录、与 plan.md 的偏差、交接说明。
> 总体技术规划见 `plan.md`，需求见 `项目要求.md`。

## 当前状态

- **当前阶段：Phase 6（待开始）**
- 最近完成：Phase 5 生词加入 + 详情自动生成管线（2026-08-03）
- **项目位置已迁移：`D:\project\my-translate`**（2026-08-03，原 `D:\项目\my-translate` 已删除，彻底规避中文路径风险）
- **已接入 git 版本管理**：初始基线提交 `8995c89`（Phase 1-3）；P4/P5 提交见 git log
- 环境：Windows 11 / Node v24.14.1 / pnpm 9.15.9（corepack 启用）

## 阶段总览

| 阶段 | 内容 | 状态 | 完成日期 |
|---|---|---|---|
| P1 | 工程基座（electron-vite + React18 + TS + Tailwind v3 + shadcn/ui） | ✅ 完成 | 2026-08-03 |
| P2 | 窗口/托盘/设置骨架 + LLM 客户端 + 冒烟打包 | ✅ 完成 | 2026-08-03 |
| P3 | sql.js 数据层（持久化/迁移/IPC） | ✅ 完成 | 2026-08-03 |
| P4 | 翻译 + 原文 Chip 交互 | ✅ 完成 | 2026-08-03 |
| P5 | 生词加入 + 详情自动生成管线 | ✅ 完成 | 2026-08-03 |
| P6 | 生词本 UI + 深浅色 + 全量打磨 | ⬜ 未开始 | |
| P7 | 正式打包与交付 | ⬜ 未开始 | |

---

## Phase 1 完成记录

### 验收结果（全部通过）

| 验收项 | 结果 |
|---|---|
| `pnpm install` 无原生编译（无 node-gyp） | ✅ 1m36s，仅 electron/esbuild postinstall 下载二进制 |
| `pnpm typecheck`（node + web 双配置） | ✅ 通过 |
| `pnpm build` 三端产物 | ✅ out/main、out/preload（CJS）、out/renderer |
| 构建产物启动 | ✅ `electron.exe .` 窗口正常打开并保持运行 |
| `pnpm dev` 开发模式 | ✅ dev server :5173，Electron 成功拉起 |
| shadcn Button / sonner / zustand / 深色切换 / window.api | ✅ 验证页已渲染（App.tsx） |

### 已建立的关键结构

- `electron.vite.config.ts`：三端配置；renderer 别名 `@`→`src/renderer/src`、`@shared`→`src/shared`；生产构建注入 CSP 插件（`apply: 'build'`，dev 不注入以免阻塞 HMR）
- `src/shared/types.ts`：**完整类型契约已先行定义**（VocabEntry/AppSettings/RendererApi 等，含 Phase 2+ 方法签名注释）；`src/shared/ipc.ts`：全部 IPC 频道名常量
- `src/main/index.ts`：最小主窗口（400×560，min 340×420，sandbox+contextIsolation），P2 在此扩展
- `src/renderer`：globals.css（shadcn slate 双主题变量）、`ui/button.tsx`、`ui/sonner.tsx`（**已去除 next-themes 依赖**）、`stores/uiStore.ts`（zustand + class 式深色切换）、App.tsx 为 P1 验证页（P2 替换）
- `components.json`：shadcn 配置已就位，后续可 `pnpm dlx shadcn@3.x add <组件>` 增量添加（注意：CLI 新版本默认 Tailwind v4 语法，添加组件后需人工核对生成代码与 v3 配置兼容）

### 与 plan.md 的偏差（重要）

1. **主进程模块格式：ESM → CJS**。原因：sandbox preload 必须为 CJS，main 同步采用 CJS 保持单一模块体系，是 electron-vite v2 最成熟路径；避免 ESM 主进程 + CJS preload 混编的扩展名/路径坑。
2. **配置存储：electron-store → 自写 JSON 存储**（plan 已允许二选一）。electron-store v8+ 为 ESM-only，与 CJS 主进程冲突；P2 将在 `src/main/settings.ts` 手写 settings.json 读写（userData 目录）。
3. **`.npmrc` 启用 npmmirror**：`electron_mirror` + `electron_builder_binaries_mirror`（国内网络，如不需要可删）。
4. **pnpm 9.15.9 + `pnpm.onlyBuiltDependencies: ["electron","esbuild"]`**：pnpm 9 默认阻止 postinstall，必须显式允许，否则 Electron 二进制不会下载。
5. CSP 未写入 index.html 源码，改为构建期注入（dev 模式 react-refresh 内联脚本与 `script-src 'self'` 冲突）。

### 注意事项 / 已知事项

- 项目路径已迁移至纯 ASCII 路径（`D:\project\my-translate`），中文路径风险解除（历史：原 `D:\项目\` 路径下 install/build/dev/打包均正常，仅打包偶发 ENOENT）。
- React 18.3.1 + Tailwind 3.4.x + Vite 5.4 + Electron 33，均按计划锁定，不要随意升大版本。
- 冒烟测试方法：`Start-Process electron.exe .` 后检查进程存活；退出用 `taskkill /PID <id> /T /F` 清整棵进程树。

---

## Phase 2 完成记录

### 验收结果

| 验收项 | 结果 |
|---|---|
| `pnpm typecheck` / `pnpm build` | ✅ 通过 |
| `pnpm dev` 冒烟 | ✅ 无边框窗口启动，无 stderr 报错 |
| NSIS 冒烟打包 `pnpm dist` | ✅ 产出 `release/My Translate-Setup-0.1.0.exe`（86.5MB）+ win-unpacked |
| 打包模式冒烟运行 | ✅ `win-unpacked/My Translate.exe` 启动正常（托盘创建成功=tray.ico extraResources 路径正确） |
| 关闭→托盘 / 置顶 / 透明度 / bounds 记忆 / 设置持久化 | ⚠️ 代码就绪，交互行为需人工目视确认（见下「待人工验收」） |

### 已实现内容

- **主进程**
  - `settings.ts`：自写 JSON 配置存储（userData/settings.json，deepMerge 局部更新，300ms 防抖 + 原子写 tmp→rename，损坏自动备份，`flushSettings()` 退出前强制落盘）
  - `window.ts`：无边框窗口（400×560 / min 340×420）、bounds 记忆与恢复（`boundsVisible` 校验落在某屏工作区内）、close 拦截→隐藏（`quitting` 标记放行真退出）、置顶/透明度应用原语
  - `tray.ts`：托盘（**全局持有防 GC**）、左键切换显隐、菜单「显示/隐藏 · 始终置顶(checkbox) · 退出」；托盘切置顶会广播 `WindowAlwaysOnTopChanged` 同步渲染端图钉
  - `llm/client.ts`：OpenAI 兼容 `POST {baseURL}/chat/completions`（Node fetch + AbortController 超时）、HTTP 状态码 → 中文错误文案（401/404/429/5xx）、`testConnection`（max_tokens=1，15s 超时）；`normalizeBaseURL` 去尾斜杠，**/v1 由用户自行保证**（设置页有提示）
  - `ipc.ts`：全部 handler；窗口类「编排逻辑」（改设置→作用窗口→刷托盘菜单→广播渲染端）集中在此，window/tray 只有原语，**单向依赖无循环**
  - `index.ts`：单实例锁（第二实例聚焦已有窗口）、before-quit → setQuitting + flushSettings；**不监听 window-all-closed**（常驻托盘）
- **渲染端**：TitleBar（drag 区 + no-drag 按钮：置顶图钉/深浅色/最小化/关闭）；底部导航三视图切换；SettingsView（LLM 表单 + 保存 + 测试连接[先存后测] + 置顶 Switch + 透明度 Slider[50-100%]）；settingsStore（乐观更新）；新增 shadcn 组件 input/label/switch/slider/card
- **资源/打包**：`build/tray.ico`（32）+ `build/icon.ico`（256）由 PowerShell System.Drawing 生成；`electron-builder.yml`（appId/productName/files 仅 out/**、extraResources 带 tray.ico、NSIS assisted+perMachine:false）；scripts 新增 `dist` / `dist:dir`
- **新增依赖**：@radix-ui/react-label / react-switch / react-slider

### 关键设计（后续阶段不要破坏）

- 退出唯一入口=托盘「退出」→ `forceQuit()`；渲染端「关闭」按钮 = `win.close()` → 拦截隐藏
- 设置写入唯一通道 `updateSettings()`（防抖落盘）；退出前必须 `flushSettings()`
- dev 与打包的 userData 不同名：dev=`%APPDATA%/my-translate`，打包=`%APPDATA%/My Translate`（互不影响，属预期）
- 未签名：SmartScreen 警告属预期，P7 在 README 说明

### 待人工验收（用户目视）

1. `pnpm dev`：拖动标题栏移动窗口；拖边缘缩放（无边框缩放实测）；图钉置顶；关闭按钮→窗口消失但进程在（托盘图标在）；托盘菜单「退出」可真正退出
2. 设置页：填 LLM 配置→保存→**重启应用仍在**；透明度滑块实时生效
3. 托盘菜单勾选「始终置顶」→ 标题栏图钉同步高亮

---

## Phase 3 完成记录

### 验收结果

| 验收项 | 结果 |
|---|---|
| SQL 语义独立验证（node + 真实 sql.js） | ✅ 12/12：建表版本/去重(含大小写)/checkWords/LIKE 转义/双排序/导出重载回读/备注更新/删除 |
| `pnpm typecheck` / `pnpm build` | ✅ 通过 |
| dev 冒烟 run1（新建） | ✅ 进程存活，`%APPDATA%\my-translate\my-translate.db` 生成（20KB 空库），无 corrupt |
| dev 冒烟 run2（重启加载） | ✅ 进程存活，db mtime 未变=走加载路径未重写，无 corrupt |
| 打包版冒烟（`pnpm dist:dir` + win-unpacked） | ✅ 进程存活，`%APPDATA%\My Translate\my-translate.db` 生成（resourcesPath wasm 加载成功） |

### 已实现内容

- `db/database.ts`：wasm 双路径（dev=`app.getAppPath()/node_modules/...`、packaged=`process.resourcesPath`）+ `wasmBinary` 直读传入；MIGRATIONS 数组 + `PRAGMA user_version` 逐版本迁移；损坏文件备份 `.corrupt.<ts>` 后重建；`initDatabase()` 返回 `{needFlush}`
- `db/persist.ts`：`markDirty`（800ms 防抖）+ `flushDatabase(force)`（export → tmp → rename 原子写）
- `db/vocabRepo.ts`：list（搜索 LIKE 转义 `%_` + time/alpha 排序）、checkWords（IN 批量小写）、add（INSERT OR IGNORE + getRowsModified 判去重）、updateVocabNote、deleteVocab、updateVocabDetail（P5 回写预留）
- IPC：VocabList/CheckWords/Add/UpdateNote/Delete + `VocabChanged` 广播；preload 与 RendererApi 契约同步补齐
- `package.json` 增加 `"productName": "My Translate"`（见下「关键发现」）
- electron-builder.yml extraResources 增加 sql-wasm.wasm

### 关键发现与教训（务必保留在记忆中）

1. **单实例锁被残留进程占用的排查**：P2 冒烟启动的 `win-unpacked/My Translate.exe` 未被真正杀死（`Get-Process electron` 按进程名过滤漏掉了它），长期占用单实例锁 → 所有后续 dev/产物运行拿到 `requestSingleInstanceLock: false` 直接 `app.quit()`，表现为「无报错、无 db 文件」。**教训：杀进程必须用 `Get-CimInstance Win32_Process` 按 CommandLine/Name 过滤；冒烟结束要清 `My Translate.exe` 残留。**
2. **`flushDbFile()` 忘传 force 参数**：新建库时 dirty=false 提前 return，db 文件永不落盘。启动路径必须 `flushDbFile(true)`。
3. **Windows 上 Electron main 进程 console.error 不可见**（GUI 子系统，输出不接终端）。调试必须写文件日志（如 userData/debug.log）。本次正是靠临时文件日志定位问题，定位后已全部移除。
4. **打包版 userData 实测=小写 `my-translate`**（electron-builder.yml 的 productName 不改 app.getName()）→ 与 dev 共享 userData/单实例锁。已通过在 package.json 加 `productName` 解决：现在 dev=`my-translate`、打包=`My Translate`，彻底分离。
5. **AUTOINCREMENT + INSERT OR IGNORE 会消耗序列号**（被忽略的插入也自增）→ id 有空洞，属 SQLite 正常行为，应用无感知，不做处理。
6. 迁移前中文路径打包偶发 ENOENT：首次 `dist:dir` 失败（残留进程锁文件），清进程后重跑成功；P7 若再遇诡异路径错误，先清进程再试（路径问题现已因迁移而规避）。

### 待人工验收（用户目视）

1. `pnpm dev` 后：生词本页（仍为占位）不影响；重启应用 db 不丢
2. 完整 IPC 链路（加词→杀进程→重启→数据在）需等 P5 UI 完成后验证；SQL 层已独立验证

---

## Phase 4 完成记录

### 验收结果

| 验收项 | 结果 |
|---|---|
| 流式 SSE 解析独立验证（真实代码 bundle + mock fetch） | ✅ 10/10：正常流（含 [DONE]/空行/keep-alive/推理字段忽略）/401 文案/缺 Key 引导/用户中止静默/超时文案 |
| `pnpm typecheck` / `pnpm build` | ✅ 通过 |
| dev 冒烟 | ✅ 4 个 electron 进程存活，无 stderr 报错 |
| 真实 LLM 翻译 | ⚠️ 用户已确认设置页「测试连接」成功（aihubmix），流式翻译效果待用户目视 |

### 已实现内容

- **主进程** `llm/translate.ts`（不依赖 electron，可独立测试）：SSE 逐行解析（`data:` 前缀 / `[DONE]` / 空行与注释行忽略 / `delta.content` 累加、`reasoning_content` 等无关字段忽略）；requestId → AbortController 映射；用户中止静默 vs 超时（120s）提示区分；错误统一走 emit.error
- **契约**：`TranslateEvent` 联合类型；RendererApi 补 `translate` / `abortTranslate` / `onTranslateEvent`（preload 三频道聚合转发 + 清理）
- **渲染端**：
  - `lib/tokenize.ts`：`/[A-Za-z]+(?:[''’\-][A-Za-z]+)*/g`（don't / state-of-the-art / 弯引号）；非词片段原序保留；`wordList()` 小写去重
  - `stores/translateStore.ts`：状态机 idle/loading/streaming/error；流式追加；selectedWords（Set 小写归一）；existedWords（翻译完成自动 checkWords 标记）；输入变化自动清空选择；Ctrl+Enter 触发翻译
  - `WordChips.tsx`：单词 Chip 多选高亮；已收录 Chip 划线弱化 + ✓ 角标（点击提示）；选中 ≥1 显示「加入生词本(n)」
  - `AddConfirmDialog.tsx`：shadcn Dialog 二次确认，按原文顺序列词，加入后 toast（含已存在跳过统计）
  - `TranslateView.tsx`：输入区 + 翻译/停止/清空 + 错误横幅（未配置 Key 时含「去设置」按钮）+ 译文卡片
  - 新增 shadcn 组件：`textarea` / `dialog`（@radix-ui/react-dialog）
- **App.tsx**：挂载时订阅翻译事件（视图切换不丢失）

### 关键发现与教训

1. **目录迁移后必须 `pnpm install` 修复虚拟 store**：Move-Item 后 pnpm 报 `ERR_PNPM_UNEXPECTED_VIRTUAL_STORE`（node_modules 内链接仍指向旧路径）。修复：`pnpm install --config.confirm-modules-purge=false`（跳过交互确认，从 store 复用缓存，10s 完成）。
2. **Node 24 的 AbortSignal 实例没有 `.abort()` 方法**（只有 AbortController 有）——测试 mock 中直接 `signal.abort()` 会抛 "not a function"。应用代码用的 `controller.abort()` 无此问题。
3. **bash 工具沙箱偶发引用旧路径**（`NotFound: FileSystem.access (D:\项目\my-translate)`）：与 node 子进程间歇性相关，换 `workdir=D:\project` 可绕过，与项目代码无关。
4. 首次尝试 `npx esbuild` 打包测试 bundle 也踩到旧路径问题，改用 node -e 直调 esbuild API 稳定。

### 待人工验收（用户目视）

1. 输入英文 → 翻译 → 流式打字机效果；停止按钮可中断
2. 原文 Chip 多选 → 「加入生词本(n)」→ 确认 → toast；已收录词第二次翻译显示划线 + ✓
3. 生词加入后可在 `%APPDATA%\my-translate\my-translate.db` 用 SQLite 工具查看到（pending 状态行）
4. 未配置 Key 时翻译 → 错误横幅 + 「去设置」按钮

### 追加记录（2026-08-03，用户反馈「仍是占位页」排查）

- **根因**：用户打开的是 `release/win-unpacked` 里 **P3 时代的旧打包版**（当时 TranslateView 还是占位页）。`pnpm dev` 服务的一直是新代码（已抓取 dev server 模块确认）。
- **衍生问题**：旧 `release/` 目录的 `resources/app.asar` 被某进程持续锁定（反复删除失败，怀疑 Windows Search 索引器，无打包相关进程在跑），导致 `pnpm dist` 无法在 `release` 输出。**处置：已改用 `release2` 输出目录完成新构建（win-unpacked 运行验证 4 进程存活 + NSIS 安装包 `release2/My Translate-Setup-0.1.0.exe` 86.9MB）**。旧 `release/` 待用户手动删除（关掉资源管理器窗口或重启后删）。
- **临时打包命令**：`pnpm exec electron-builder --config.directories.output=release2`；待旧目录删除后恢复 `pnpm dist`。
- **教训**：向用户交付验证时，必须明确「用 `pnpm dev` 看新代码；release 产物是旧的」。以后每个 Phase 结束后如要交付安装包，需确认 release 目录无锁或换输出目录。
- `.gitignore` 已补 `release2/`。

---

## Phase 5 完成记录

### 验收结果

| 验收项 | 结果 |
|---|---|
| wordDetail 容错 JSON 解析独立测试（真实代码 bundle） | ✅ 33/33：普通 JSON / 代码块包裹 / 前后解释文字 / 字段缺失兜底 / 非 JSON 全空 / examples 结构异常过滤 / Markdown 模板逐节匹配 / 多组大括号截取 |
| COALESCE 部分更新 + last_insert_rowid（真实 sql.js 独立验证） | ✅ 4/4：成功回写全字段 / 失败回写保留字段 / 置回 pending 清空原因 / rowid 可读 |
| 真实 LLM 生成一条详情（aihubmix, gpt-4o-mini） | ✅ 3.4s 出结果，Markdown 五节结构与需求模板完全一致 |
| `pnpm typecheck` / `pnpm build` | ✅ 通过 |
| dev 冒烟 | ✅ 4 个 electron 进程存活，无 stderr 报错 |

### 已实现内容

- **`llm/prompts.ts`**（新）：翻译 prompt 从 translate.ts 迁入（消除重复定义）；`buildDetailSystemPrompt()` 严格 JSON 指令（phonetic/pos/brief/usage/examples[{en,zh}×3]/synonyms/antonyms/memory，中文字段说明，禁代码块标记）；`buildDetailUserPrompt(word, contextSentence)` 附原句消歧
- **`llm/wordDetail.ts`**（新）：`generateWordDetail` 复用 client.chatCompletion（maxTokens 800 / timeout 60s）；**容错解析链**：`extractJsonObject` 截取首个 `{` 至末个 `}` → JSON.parse → 逐字段兜底空值（`parseDetailJson` 任何失败路径返回全空，不抛异常）→ `hasAnyContent` 判定；全空时抛 LlmError「无法解析，请重新生成」；`buildDetailMarkdown` 组装需求模板五节
- **`llm/client.ts`**：`LlmError` 增加 `status?: number`，chatCompletion 的 `!res.ok` 分支附上 HTTP 状态码（队列 429 退避判据）
- **`db/vocabRepo.ts`**：`addVocab` 返回 `addedIds`（last_insert_rowid）；新增 `getVocabById` / `resetVocabToPending`；`updateVocabDetail` 改为 **COALESCE 部分更新**（`patch.xxx ?? null` → `COALESCE(?, 原列)`，失败回写不清已有字段）
- **`services/vocabService.ts`**（新）：串行队列——`enqueueDetail(id)` 幂等（inQueue 去重 + 仅收 status='pending'）；pump 逐词处理间隔 300ms；429 指数退避重试 ≤2 次（2s/4s）；成功 `{status:'ready'}` 回写，失败 `{status:'failed', failReason}` 保留字段；每词完成 `notify()`（回调由 ipc.ts 注册 → 广播 VocabChanged，避免循环依赖）；`enqueueAllPending()` 启动扫描崩溃恢复；`regenerateDetail(id)` 失败/已 ready 词重生成入口
- **`ipc.ts`**：VocabAdd handler 对 addedIds 逐个 `enqueueDetail`（不 await，后台跑）；新 handler `LlmRegenerateDetail`；`setVocabChangedListener(broadcastVocabChanged)` 接线
- **`index.ts`**：whenReady 末尾 `enqueueAllPending()`（必须在 registerIpcHandlers 之后，监听器已就绪）
- **契约/preload**：`VocabAddResult.addedIds: number[]`；`RendererApi.regenerateDetail(id)`（去掉 P4 占位注释）；preload 补齐 invoke

### 关键发现与教训

1. **node 直连 LLM 失败 ≠ 应用不可用**：本机走系统代理 127.0.0.1:7897，PowerShell shell 无 HTTP_PROXY 环境变量 → Node fetch/curl 全部 connection failed（000），但 Electron 主进程走 Chromium 网络栈（自动跟随系统代理）正常。**验证 LLM 链路时若 shell 直连失败，先 `$env:HTTPS_PROXY='http://127.0.0.1:7897'` 再跑**，不要误判为代码问题。
2. **真实详情生成 3.4s 返回**：单条详情约 600-900 token，在队列 300ms 间隔下 10 词 ≈ 40s 完成，体验可接受；若用户模型较慢可后续调整 maxTokens。
3. 队列幂等的另一道保险：`enqueueDetail` 对**非 pending** 状态直接忽略（即使 inQueue 无记录），与 UI 侧「已 ready 不重复入队」语义一致。

### 待人工验收（用户目视，P6 生词本 UI 完成后统一验证）

1. 翻译页选词 → 确认加入 → toast「已加入 n 个生词，正在生成详情…」→ 每词依次完成（间隔 300ms），全部完成后 DB 中对应行 status='ready'、detail 为五节 Markdown
2. 断网/错 Key 时加词 → 词条保留（pending/failed），失败行 fail_reason 有中文原因，P6 UI 提供「重新生成」
3. 生成中途杀掉进程 → 重启后 pending 词自动重新入队补齐
4. 同一词第二次加入 → 跳过（existed），不重复生成

---

## 交接说明（Phase 6 开工前必读）

**Phase 6 目标**（plan.md §5）：生词本完整 UI——VocabView（搜索/排序/卡片/展开/备注/删除确认/失败重试）、react-markdown prose 渲染 detail、深浅色全部组件过一遍、全部异步操作 loading/toast/空态。

**具体任务拆解**：
1. `stores/vocabStore.ts`：list/search/sortBy/expandedId/loading；挂载时 `listVocab` + 订阅 `onVocabChanged`（增删与详情生成完成都会广播，**注意区别**：广播时静默重新拉取即可，toast 只在用户操作处弹）
2. `VocabView.tsx` + `VocabToolbar.tsx`：搜索框（防抖 300ms，可复用 settingsStore 的 debounce 模式或自写）、排序下拉（time/alpha，设置项 vocab.sortBy 可持久化，P5 未动 settings 契约，可后补）
3. `VocabCard.tsx`：word/phonetic/brief/相对时间/status 徽标（pending=生成中 skeleton 动画、failed=红点 + 重试按钮）；点击展开 `VocabDetail`
4. `VocabDetail.tsx`：react-markdown + remark-gfm 渲染 detail（`@tailwindcss/typography` prose，深色 `dark:prose-invert`，依赖已装）；备注 textarea（失焦或保存按钮 → `updateVocabNote`）；删除需二次确认 Dialog（复用 AddConfirmDialog 模式）；failed 词「重新生成」按钮 → `window.api.regenerateDetail(id)`（P5 已就绪）
5. 深浅色：所有新增组件对照现有 shadcn 变量写，避免硬编码颜色
6. 空态/加载：无词时引导文案；首次进入列表 loading 骨架
7. **验证**：P5 加的 pending/ready/failed 三条数据手动过一遍全流程；`pnpm typecheck` + `pnpm build` + dev 冒烟

**注意**：renderer 侧 `VocabEntry.status` 字段 P5 已就绪（shared/types.ts），Card 徽标直接读；`regenerateDetail` IPC 已通；不要改主进程契约，本阶段纯 UI。

---
