请在现有项目基础上，新增并完善以下功能：

### 1. 支持 Anthropic 规则的 API

- 在设置页增加 API 类型选择：OpenAI 兼容 / Anthropic
- 当选择 Anthropic 时，使用 Anthropic 的 messages 格式调用（支持 Claude 系列模型）
- 配置项保持统一：Base URL、API Key、Model 名称
- 两种 API 类型的配置独立保存，切换后立即生效

### 2. 固定短语选择功能

- 在原文单词 Chip 区域，支持**按住鼠标左键并拖动**选中连续相邻的单词
- 拖动过程中实时高亮选中的连续单词，形成短语
- 松开鼠标后，可将该短语整体加入生词本
- 短语与单词共用同一套加入逻辑和详情生成流程

### 3. 极简模式

- 增加「极简模式」开关（建议放在窗口右上角或设置中）
- 开启极简模式后：
  - 界面只保留输入框和必要的操作按钮
  - 翻译完成后，原句直接被翻译结果替换
  - 提供「原句 / 译文」切换按钮，可在两者之间快速切换
- 关闭极简模式后恢复完整界面（原文 Chip + 翻译结果同时显示）
- 极简模式状态需要持久化

### 4. 生词计数与频率排序

- 在 Vocabulary 表中增加 count 字段（默认值为 1）
- 当用户再次加入已经存在的单词或短语时，不创建新记录，而是将该记录的 count 值 +1，并更新 updatedAt
- 生词本列表支持按「出现次数（count）」降序排序
- 卡片上清晰显示该词的出现次数

请优先保证以上功能的稳定实现和良好交互体验，代码风格与现有项目保持一致。